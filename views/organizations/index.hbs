{{#if organizations}}
	<ul>
	{{#each organizations}}
		<li><a href="/organizations/{{this.name}}" title="{{this.name}}">{{this.name}}</a></li>
	{{/each}}
	</ul>
{{else}}
	<p>Sorry, there are no public organizations available... <a href="/organizations/create">Create One</a></p>
{{/if}}