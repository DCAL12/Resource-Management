{{!-- Handlebars Sitewide Layout -- make changes here that will apply to the entire site --}}
<!DOCTYPE html>
<html>

<head lang="en">
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <meta name="description" content="{{config.siteDescription}}">
    <link rel="stylesheet" media="screen" href="/stylesheets/style.css">
    <!-- Adds HTML5 element support for earlier versions of IE -->
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
</head>

<body>
    <header role="banner">
        <h1><a href="/"  title="{{config.siteTitle}}">{{config.siteTitle}}</a></h1>
        <div id="accountInfo">
            <label for="accountInfo">Account: </label>
            {{#if userName}}
                <a href="/users/account" title="Modify Account">{{userName}}</a>&nbsp;|
                <a href="/users/logout" title="Logout">Logout</a>
            {{else}}
                <a href="/users" title="Login">Login</a>&nbsp;| 
                <a href="/users/create" title="Create Account">Create Account</a>
            {{/if}}
        </div>

        <nav role="navigation" class="siteNavigation">
            <ul>
                {{!-- Application-wide navigation menu goes here. Each link should be included as a <li> --}}
                {{!-- <li><a href="{{this.route}}" title="{{this.title}}">{{this.title}}</a></li> --}}
            </ul>
        </nav>
    </header>

    <main role="main" class="{{className}}">
        <h2>{{title}}</h2>
        {{{body}}}
    </main>

<footer>
</footer>

</body>
</html>