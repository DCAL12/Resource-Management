{
  "name": "Resource-Management",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www"
  },
  "dependencies": {
    "bcrypt": "^0.8.1",         // hash passwords for secure storage
    "body-parser": "~1.12.0",
    "connect-flash": "^0.1.1",  // attach messages to request.body
    "connect-mongo": "^0.8.0",  // configure express-session to use mongo for session storage
    "cookie-parser": "~1.3.4",
    "debug": "~2.1.1",
    "express": "~4.12.2",
    "express-session": "^1.10.4",
    "hbs": "^3.0.1",            // handlebars view-engine
    "mongoose": "~3.8.21",      // use schemas with mongodb
    "morgan": "~1.5.1",         // logging
    "passport": "^0.2.1",       // user authentication module
    "passport-local": "^1.0.0"  // uses username/password authentication
  }
}
